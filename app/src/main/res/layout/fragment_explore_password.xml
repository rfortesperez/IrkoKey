<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".presentation.modules.explorePasswords.ExplorePasswordFragment">

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabBackground="@color/md_theme_primary"
        app:tabIndicatorAnimationMode="linear"
        app:tabIndicatorColor="@color/md_theme_onPrimary"
        app:tabIndicatorGravity="bottom"
        app:tabTextColor="@color/md_theme_onPrimary"
        app:tabSelectedTextAppearance="@color/md_theme_primary"
        app:tabIconTint="@color/md_theme_onPrimary"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@color/md_theme_onPrimaryContainer" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager2"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"/>
</LinearLayout>